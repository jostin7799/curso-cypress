//Nazareth Agüero Alvarado
import { HomeMethods } from "./pages/login/home/home.methods"
import { Logger } from "./util/logger"
import { LoginMethods } from "./pages/login/login.methods"


/*describe('template spec', () => {
  it('passes', () => {
    cy.clearCookies()
    const usuario = "random01";
    const contrasena = "random01";

    Logger.stepNumber(1)
    Logger.step("Navigate to Demoblaze page")
    cy.visit('https://www.demoblaze.com/index.html')


    Logger.stepNumber(2)
    Logger.step('Click on "Login" link')
    cy.get('a[data-target="#logInModal"]').click()

    Logger.stepNumber(3)
    Logger.step('Login with this credentials: ' + usuario + ' and ' + contrasena)
    LoginMethods.login(usuario, contrasena)
    Logger.verification('The Home should show "welcome" ' + usuario + 'text')
    cy.get("a#nameofuser").should("contain.text", usuario)
    cy.wait(10000)
  })
})*/